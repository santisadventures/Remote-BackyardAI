# Copy to .env and fill in values.
#
# Recommended: bind the SSH gateway only to your Tailscale IP (100.x).
# On macOS:
#   export TS_IP="$(tailscale ip -4)"
#   docker compose up -d --build
TS_IP=100.x.y.z

# Gateway user + public key (public key only; never commit private keys).
GATEWAY_USER=friend1
GATEWAY_PUBLIC_KEY=ssh-ed25519 AAAA... friend1@termius

